
app.clientCreate = function(events) {

	var
		/*sm = app.stateCreate('client', {
			initial: 'disconnected',
			events: [
			]
		}),*/

		rec = app.recCreate(events, app.cfg.recCfg),


		p = {}

	;





	return {
		p: p
	};
};