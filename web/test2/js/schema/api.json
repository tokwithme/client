{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "API",
	"description": "abc",

	"type": "object",

	"patternProperties": {
		"^join$": {"$ref": "joinReqSchema#"},
		"^leave$": {"$ref": "leaveReqSchema#"}
	},

	"required": []
}